# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its algorithms focus without claiming live deployment or external usage.

## Cases

- `baseline`: `input width`, score 144, lane `ship`
- `stress`: `search depth`, score 199, lane `ship`
- `edge`: `boundary pressure`, score 148, lane `ship`
- `recovery`: `complexity`, score 162, lane `ship`
- `stale`: `input width`, score 198, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
